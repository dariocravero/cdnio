%h2 GET '/' 
%h3 Params
%ul
  %li
    %strong provider (optional):
    %ul
      %li all (default)
      %li cdnjs
      %li google
      %li microsoft

%p Gets the latest library for a provider. If there's none, it will throw a 404.

%h3 Examples

%strong
  %code ➜  curl http://cdnio.uxtemple.com -H "Accept: application/json" -H "Content-type: application/json"
%p Response:
%code=File.read(File.join(File.dirname(__FILE__), 'libraries.out')) 

%br
%br
%strong
  %code ➜  curl http://cdnio.uxtemple.com\?provider\=google -H "Accept: application/json" -H "Content-type: application/json"
%p Response:
%code=File.read(File.join(File.dirname(__FILE__), 'libraries-google.out')) 

=haml :latest
